module.exports = {
  content: ['./src/**/*.{js,jsx,ts,tsx}'],
  theme: {
    extend: {
      colors: {
        foreground: '#374151', // Example gray color, adjust as needed
        // add other custom colors you use, e.g. background, card, etc.
      },
    },
  },
  plugins: [],
};
